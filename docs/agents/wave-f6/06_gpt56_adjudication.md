# Wave-F6: Adjudication of the external GPT-5.6 audit

**Adjudicated:** 2026-07-10, against branch `claude/friendly-ramanujan-nMWNv` (HEAD 1b11bea).
**Critical context:** the external audit reviewed commit `e054a50` — the OLD default branch, ~40 Wave-F5 commits behind this branch — and 3-5-day-old runtime logs from that old deployment. Every finding below was re-verified against CURRENT code by grep/read/compile; nothing was accepted or dismissed on the audit's word alone.

Verdict legend: **ALREADY_FIXED** (closed pre-adjudication, cite), **STILL_VALID** (confirmed on current code), **PARTIAL** (part fixed, part open), **FALSE_POSITIVE_STALE** (only true on e054a50), **OUT_OF_SCOPE** (roadmap/architecture, not a bug).

## Adjudication table

| # | Finding | GPT sev | Verdict | Current-code evidence | Wave-F5 / prior ref | Action needed |
|---|---|---|---|---|---|---|
| 1 | Release identity / source drift: 10 vs 30 services; `execution_gateway/gateway.py`, `advisor`, `polymarket`, `clmm_lp`, `basis_desk` dirs "absent" | P0 | **FALSE_POSITIVE_STALE** (dirs) + **OUT_OF_SCOPE** (release pinning) | All exist on this branch: `modules/execution_gateway/gateway.py`, `modules/advisor/`, `modules/polymarket/`, `modules/clmm_lp/`, `modules/basis_desk/`; `main.py` registers the full expansion-wave set. GPT audited a checkout missing the module-expansion + F5 waves — its "unknown deployment" was simply this branch running | Module-expansion wave (2026-06-13), Wave-F5 | Deploy from ONE branch; adopt image-digest/SHA-at-startup as roadmap. No code bug |
| 2 | Public dashboard on plain HTTP, `0.0.0.0`, port published | P0 | **STILL_VALID** (ops posture) | `modules/dashboard/main_dashboard.py:206` binds `0.0.0.0`; `docker-compose.yml:90` publishes `8080:8080`. Mitigations already in code: global auth middleware (`auth/middleware.py:116` — all non-public routes 401 without session), CSRF (`auth/csrf.py`), scheme-aware Secure cookies (`DASHBOARD_HTTPS=auto`, `monitoring/auth_routes.py:28`) | Scheme-aware sliding session cookie (Wave-F5 advisor item) | **Operator action**: firewall/VPN 8080 or TLS reverse proxy. Code can't fix a public bind decision; consider `DASHBOARD_BIND_HOST` env default `127.0.0.1` inside compose networks |
| 3 | Kill-switch poller not started in most entrypoints; `logs/.killswitch` won't stop live writes | P0 | **PARTIAL — 3 real gaps** | `should_skip_live` still reads only the in-memory flag (`core/dry_run.py:151`); pause files ARE stat'd per call (`:153`). Pollers now DO start in: sniper (`main_sniper.py:86`), arbitrage (`main_arbitrage.py:366`), dex (`main_dex.py` + BaseModule), copy (`CopyTradingEngine(BaseModule)` start-wrap, `modules/base_module.py:148`), and all 16 expansion-wave mains. **Still missing: FUTURES (`main_futures.py` uses plain `FuturesTradingEngine`, no poller), SOLANA (`modules/solana_trading/` — zero hits), AI (`modules/ai_analysis/` — zero hits)** | BaseModule auto-wrap; per-main pollers added across waves | **Wave-F6 P0 fix**: add `start_killswitch_poller()` to `main_futures.py`, `main_solana.py`, `main_ai.py` (3 one-liners), or make `should_skip_live` stat the file directly. Add the process-level kill test GPT proposed |
| 4 | State-changing routes lack `require_admin`; viewer session can trade/configure | P0 | **STILL_VALID** | Role system exists (`auth/middleware.py:61 require_admin`, `:84 require_role`) but applied to only ~11 of ~59 mutating routes in `enhanced_dashboard.py` + user mgmt + bot start/stop/emergency/pause (`module_routes.py:75-93`). Unprotected (auth-only, any role): `/api/trade/execute` (:1431), `/api/position/close|modify`, `/api/order/cancel` (:1432-1434), `/api/settings/update` (:1355), `/api/config/{type}` (:1359), `/api/strategy/parameters` (:1448), `/api/ml/train` (:1458), all per-module settings/close-all/unblock (:1257-1404), module enable/disable/pause/start (:1777-1780), `module_routes.py:85-97` start/enable/disable/reallocate, and ALL 6 RPC-pool mutations (`rpc_pool_routes.py:53-60` — endpoint CRUD incl. API-key-bearing URLs) | F5 hardened auth/CSRF/session but did not do RBAC per route | **Wave-F6 P0 fix** (backend-devops): wrap every mutating route in `require_admin` (or `require_role(OPERATOR)` for bounded ops); route-authorization test |
| 5 | Generic CORS `"*"` with credentials | P0 (bundled in #4) | **STILL_VALID** | `enhanced_dashboard.py:1462-1468`: `aiohttp_cors.setup(defaults={"*": ResourceOptions(allow_credentials=True, ...)})` applied to all non-socket.io routes. Socket.IO CORS WAS tightened to env allowlist (`:478-486`, MB-26) but the generic layer wasn't. Wildcard-origin + credentials lets any website read authenticated API responses with the victim's cookie | MB-26 fixed only the Socket.IO half | **Wave-F6 P0 fix**: reuse the `DASHBOARD_CORS_ORIGINS` allowlist for aiohttp-cors defaults |
| 6 | `RiskManager.validate_trade` calls nonexistent `wallet_manager.get_available_balance()`; four modules fail closed; DEX bypasses | P0 | **PARTIAL — core defect STILL_VALID, consequences changed** | Confirmed: `core/risk_manager.py:1153` (and `:938` in `calculate_position_size`) call `self.wallet_manager.get_available_balance()`; `WalletSecurityManager` (`security/wallet_security.py:73`) has no such method, no `__getattr__`, and `wallet_manager` is never reassigned → AttributeError → outer `except` → `(False, "Validation error: ...")`. **`validate_trade` can never return True.** Fail-closed (no fund-loss hole) but blocks EVERY gated live entry: DEX `core/engine.py:1626`, solana `:3801/:4620`, sniper `:1749`, arbitrage `:895/:3088`, triangular `:841`, copy `:495/:582`, stat_arb/basis/options/clmm executors. F5 added a fail-soft `RiskManager.get_available_balance()` (`risk_manager.py:1068`, wallet→portfolio fallback) but `validate_trade` was never switched to use it. GPT's "DEX bypasses the validator" is stale — DEX now calls it (live-readiness wave) | Live-readiness wave (DEX gate), mig 105 (knobs only — did NOT fix this) | **Wave-F6 P1 fix**: change `:1153` and `:938` to `await self.get_available_balance()`; add a unit test that `validate_trade` can return True. Unit-mixing (USD/SOL/native passed as `amount`) remains a real P2 — typed-notional intent is roadmap |
| 7 | Copy EVM swap broadcasts `minOut=0` on quote failure | P0 | **PARTIAL (mostly fixed)** | `copy_engine.py:697-718`: LIVE **BUY fails closed** (`return {'success': False, 'error': 'quote failed (live buy fail-closed)'}`); **SELL deliberately proceeds** with `min_out=0` at CRITICAL log ("a panic exit must not be blocked by a flaky quote"). GPT's unconditional claim is stale | Live-readiness wave, mig 110 (minOut tolerance knobs) | Optional P2: bound sell-side exposure (e.g., last-known-price floor with generous tolerance) instead of 0. Design trade-off, documented |
| 8 | Arbitrage has no `should_skip_live` gate | P1 | **FALSE_POSITIVE_STALE** | `arbitrage_engine.py:3021` and `solana_engine.py:870` gate execution via `should_skip_live(self.dry_run, module='arbitrage')`; poller at `main_arbitrage.py:366`; `live_execution_enabled` defaults OFF (mig 097/108) | Live-readiness wave mig 108; recovery-wave econ gates | None |
| 9 | Sequential Solana arb sends (two swaps, inventory risk) | P1 | **STILL_VALID (gated design limitation)** | `modules/arbitrage/solana_engine.py:1374 _submit_via_rpc` submits TX1, waits confirmation, submits TX2 (`:1401/:1467`) — RPC fallback path when Jito bundle fails; still non-atomic. But live is default-OFF and CLAUDE.md verdict is AMBER/SHADOW with atomic-receiver scope-cut | Wave-F5 arb verdict downgrade; scope-cut documented | Keep live OFF (already is). Atomic receiver contract = roadmap; no F6 code change |
| 10 | Pause-key inconsistency (`ai_analysis` vs `.pause_ai`) | P1 | **ALREADY_FIXED** | Canonical short-key map (`enhanced_dashboard.py:2804-2894`, e.g. `'pause_keys': ('ai','ai_analysis')`); `_api_module_pause` (`:5567`) writes the SHORT key engines poll plus the legacy alias. Registration order (`:530` before `:535`) makes this handler win over `module_routes`' raw-key writer | Enhancement wave 2 ("dashboard pause writes the SHORT engine key") | Residue folded into #4: `_api_module_pause` at `:1779` is not admin-gated |
| 11 | Metrics contradictions: Solana PF 0.64 with +1248 SOL; DEX 58 total vs 84 successful | P1 | **PARTIAL** | Solana half: poisoned-PnL chain closed in F5 — PriceValidator routing, quorum-gated price jumps, and mig 140 tags historical poison `metadata.excluded=true` (`migrations/140_wave_f5_solana_repair_sniper_tuning.sql:28-37`); GPT read pre-fix logs. **DEX half STILL_VALID**: `core/engine.py:1579-1580`/`:1812-1813` increment `total_trades` AND `successful_trades` at OPEN, then `:2489`/`:2709` increment `successful_trades` AGAIN on winning close → successful > total is structural; feeds `win_rate` (`:2293`, `:3097`) which touches sizing heuristics | Wave-F5 Solana repair (mig 140A) | **Wave-F6 P2 fix**: make `successful_trades` mean "closed at profit" only (stop incrementing at open); recompute win_rate. Unified ledger = roadmap |
| 12 | RPC capacity exhausted; STARVED endpoints knowingly reused | P1 | **PARTIAL / by-design + roadmap** | Anti-starvation fallback intentionally returns a cooled-down endpoint rather than None (`config/pool_engine.py:894` "STARVED") — deliberate wave-13 choice. F5 added multi-key rotation (`HELIUS_API_KEY_2..9` etc., mig 144) + per-key budgets + 429 rotation, which attacks the root cause | Wave-13 pool fallback; Wave-F5 multi-key rotation | Roadmap: budget-aware scheduler, separate execution-vs-data quotas. Consider refusing STARVED endpoints for *broadcast* calls specifically (small targeted F6 candidate) |
| 13 | `trading/strategies/scalping copy.py` syntax error line 983; no pytest/lockfile; root container; `latest-pg14` | P1 | **STILL_VALID** | `py_compile` confirms `SyntaxError: unterminated string literal` at line 983. File is dead code (space in filename makes it unimportable; matches PM dead-code policy: `*copy*`). No `USER` directive in `Dockerfile`; `docker-compose.yml:22` uses `timescale/timescaledb:latest-pg14`; `requirements.txt` present, no lockfile | None | **Wave-F6 P2**: delete `scalping copy.py` (single-purpose commit). Non-root USER + image pinning + lockfile = roadmap items for backend-devops |
| 14 | Duplicate `main_dex.py` with different hashes | P1 (in DEX row) | **STILL_VALID (hygiene)** | Root `/main_dex.py` (md5 `12ce4f03...`) differs from `modules/dex_trading/main_dex.py` (`45985919...`); `main.py:524` uses the modules path; root copy referenced only by old docs/comments | None | **Wave-F6 P3**: verify zero importers, delete root `main_dex.py` per dead-code policy |
| 15 | Contract not verified → proceeds with warning (`core/engine.py:~2937`) | (DEX row) | **STILL_VALID** (moved) | Now `core/engine.py:3376`: "Contract not verified - proceeding with caution" in final safety checks. Other gates (honeypot, liquidity, RiskManager) still apply, and #6 currently blocks all live entries anyway | None | P2: make unverified-contract a hard block behind a DB knob (default block) once #6 unblocks live |
| 16 | Sniper: entrypoint lacks kill poller; 500-position cap; listener issues | P0/P1 (sniper row) | **FALSE_POSITIVE_STALE** | Poller: `main_sniper.py:86`. Cap: mig 140B set 25-position cap + 0.05 SOL size + daily-loss cap. Silent-zombie listener: `_startup_supervisor` self-healing + `listener_status=rpc_auth_failed` surfaced | Wave-F5 sniper items (mig 140B) | None (needs real Helius keys — operator) |
| 17 | Copy: 3/4 leaders dead 18-33 days; zero copies | P1 (copy row) | **ALREADY_FIXED (mechanism)** | Leader lifecycle (mig 092) + discovery revival & shadow sim conditionally activated (mig 141), per-key Helius budgets/rotation | Recovery wave + Wave-F5 copy items | Operator: confirm discovery is populating on the NEW deployment |
| 18 | Drift injects fake +12% funding in DRY_RUN | (Solana row) | **STILL_VALID (minor, deliberate)** | `solana_engine.py:3770-3774`: DRY_RUN substitutes a simulated funding rate when chain read unavailable ("to exercise wiring") — pollutes paper evidence exactly as GPT says | None | P3: tag such trades `metadata.simulated_funding=true` and exclude from promotion scorecards |
| 19 | Futures/DEX/etc. "losing, no edge" verdicts; module Blocked list | P1 | **OUT_OF_SCOPE / already aligned** | Matches our own posture: everything is DRY_RUN/shadow-first; F5 re-tuned futures geometry (mig 139) with "2+ weeks fresh DRY_RUN before judging"; arb self-downgraded to AMBER/SHADOW | Wave-F5 verdicts | None — evidence gates already the plan |
| 20 | Portfolio ledger, pre-trade reservation service, execution gateway adoption, data-quality SLOs, strategy state machine, phased roadmap | — | **OUT_OF_SCOPE (roadmap)** | Execution gateway exists as a library (mig 127) but "no module wired to it yet" — GPT's point that it can't be assumed as protection is fair and matches our own CLAUDE.md honesty note | Module-expansion wave | Adopt as backlog items (see roadmap list) |

## GENUINELY-NEW real bugs Wave-F6 must fix

Ordered by severity; none of these were closed by Wave-F5.

1. **[P0, security] RBAC gap on ~45 mutating dashboard routes** — any authenticated *viewer* can execute trades, close-all, edit settings/strategy params, train ML, enable/start modules, and CRUD RPC endpoints (incl. key-bearing URLs). `monitoring/enhanced_dashboard.py:1257-1898`, `monitoring/rpc_pool_routes.py:53-60`, `monitoring/module_routes.py:85-97`. Fix: `require_admin`/`require_role` at registration + route-authz test. (Owner: backend-devops-expert)
2. **[P0, security] Wildcard CORS with credentials** — `enhanced_dashboard.py:1462-1468`; reuse the `DASHBOARD_CORS_ORIGINS` allowlist already used for Socket.IO. (Owner: backend-devops-expert)
3. **[P0, safety] Kill-switch blind spots: FUTURES, SOLANA, AI subprocesses never start the poller** — `logs/.killswitch` cannot stop their live writes (pause files still work). Three one-line fixes in `main_futures.py` / `main_solana.py` / `main_ai.py`, or stat the file in `should_skip_live` (`core/dry_run.py:151`). Add the kill-within-1s integration test. (Owner: backend-devops-expert + pm tests)
4. **[P1, functional] `validate_trade` can never return True** — `core/risk_manager.py:1153` and `:938` call a method `WalletSecurityManager` doesn't have; AttributeError → permanent veto. All validate_trade-gated LIVE paths (now including DEX) are dead. Switch to the F5 fail-soft `self.get_available_balance()` (`:1068`) + unit test proving a passing path. (Owner: quant-algo-expert or market-trading-analyst for gate semantics review)
5. **[P2, metrics] DEX trade counters double-count wins** — `core/engine.py:1579-1580/:1812-1813` (increment at open) vs `:2489/:2709` (increment again at profitable close) → `successful_trades > total_trades`, corrupting `win_rate` at `:2293/:3097`. (Owner: quant-algo-expert)
6. **[P2, hygiene] Delete dead broken files** — `trading/strategies/scalping copy.py` (SyntaxError:983, unimportable) and root `/main_dex.py` (diverged duplicate; `main.py:524` uses the modules copy). Single-purpose commits after importer grep. (Owner: pm-architect-qa)
7. **[P2/ops] Dashboard ingress** — bind/publish posture (`main_dashboard.py:206`, `docker-compose.yml:90`) requires operator firewall/VPN/TLS-proxy action; optionally add a `DASHBOARD_BIND_HOST` env defaulting private.

## Legitimate roadmap items (not bugs)

- Immutable release identity: emit git SHA + image digest at startup; deploy by digest; SBOM + dependency scan. (GPT #1 — its "source drift" was stale-base, but the practice is right.)
- Typed pre-trade risk reservation (USD notional + venue/account) replacing the mixed-unit `amount` param; central portfolio ledger + one PnL service.
- Wire modules onto the existing `execution_gateway` library (mig 127) — idempotency keys, durable outbox, PENDING_RECONCILIATION order state.
- Data-quality SLOs + DEGRADED strategy state (no risk-opening on stale/degraded feeds); explicit per-source freshness telemetry.
- Budget-aware RPC scheduler; never use a STARVED endpoint for broadcast; separate market-data vs execution quotas (F5 multi-key rotation is the first step).
- Non-root container USER, pinned base images (replace `latest-pg14`), locked dependency set, pytest in CI as release gate.
- Strategy promotion state machine with pre-registered statistical + operational thresholds; automatic demotion. (Matches existing shadow-first posture; formalize it.)
- Atomic receiver contract for multi-leg arb before any live sequential path (already scope-cut; keep it that way).
- RBAC hardening phase 2: per-mutation audit records (command ID, actor, outcome); split trade.close vs trade.open approval classes.

## Verdict counts

ALREADY_FIXED: 3 (#8 arb gate, #10 pause keys, #16 sniper cluster, #17 copy leaders — counting 16/17 as one each = 4)
STILL_VALID: 6 (#2 ingress, #4 RBAC, #5 CORS, #9 sequential-arb-limitation, #13 hygiene/build, #14 dup main_dex, #15 unverified-contract, #18 drift funding — of which security: #4, #5, #2)
PARTIAL: 4 (#3 killswitch, #6 validate_trade, #7 minOut, #11 metrics, #12 RPC)
FALSE_POSITIVE_STALE: 2 (#1 source drift/missing dirs, #8, #16)
OUT_OF_SCOPE/roadmap: 2 clusters (#19, #20)

(Where a GPT row bundled several claims, sub-claims were split; see table.)
