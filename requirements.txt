# Core Dependencies
python-dotenv==1.0.0
pyyaml==6.0.1
aiohttp==3.9.1
aiohttp-cors==0.7.0
python-socketio==5.10.0
aiofiles==23.2.1
numpy==1.26.3
TA-Lib==0.6.7

# Solana - Pin httpx for compatibility, let driftpy manage solana/solders versions
httpx>=0.23.0,<0.28.0
base58==2.1.1
driftpy==0.8.80
# Note: driftpy brings in compatible solana and solders versions

# Futures Trading (CCXT)
ccxt==4.4.26



# Web3 and Blockchain
web3==6.20.4
eth-account==0.10.0
eth-utils==4.0.0
eth-typing==4.1.0
hexbytes==0.3.1
hdwallet==2.2.1
mnemonic==0.20

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1
redis==5.0.1

# Machine Learning

pandas==2.1.4
scikit-learn==1.4.0

# Data Processing
scipy==1.11.4

# Monitoring and Logging
prometheus-client==0.19.0
loguru==0.7.2

# Notifications - Version compatible with httpx
python-telegram-bot==20.7

# Security
cryptography==41.0.7
pynacl==1.5.0

# Web Framework
fastapi==0.108.0
uvicorn==0.25.0
websockets==12.0
jinja2==3.1.3
openpyxl==3.1.2

# Utilities - Let telegram-bot manage httpx version
tenacity==8.2.3
cachetools==5.3.2
python-dateutil==2.8.2
pytz==2024.1

# Data Validation
pydantic==2.5.3

# Parallel Processing
joblib==1.3.2

# Code Quality
pylint==3.0.3
isort==5.13.2

# Additional Security
bcrypt==4.1.2
pyotp==2.9.0
PyJWT

# Advisor module
yfinance>=0.2.40
anthropic>=0.40.0
requests>=2.31.0

# Advisor module -- BIST data (optional, import-guarded)
# borsapy: TradingView-backed BIST stocks/indices, ~15min delayed.
# Install to upgrade BIST from DEGRADED (yfinance) to AVAILABLE.
# Watchlist: watchlist_bist='THYAO,EREGL,GARAN,AKBNK' (bare tickers)
# Module runs correctly if absent (falls back to yfinance .IS DEGRADED).
borsapy>=0.1.0

# Advisor module -- TEFAS Turkish fund data (optional, import-guarded)
# tefas-crawler: 2026 tefas.gov.tr/api/funds/ JSON API, up to 5y history.
# Installs as 'tefas' package. Use: from tefas import Crawler
# Install to upgrade MIDAS_FUNDS from DEGRADED (scrape) to AVAILABLE.
# Module runs correctly if absent (falls back to scrape/manual).
tefas-crawler>=1.0.0
# tefasfon: alternative lightweight wrapper (secondary fallback).
# Module runs correctly if absent.
tefasfon>=0.1.0

# Advisor module -- vectorbt backtesting (optional, heavy, import-guarded)
# Enables vbt_backtest.run_strategy() for full equity-curve backtests.
# Heavy dep (~500 MB with numpy/pandas/scipy transitive deps).
# Module runs correctly without it; portfolio_engine.backtest() is always available.
# Uncomment to install (or add to a separate requirements-extras.txt):
# vectorbt>=0.26.0

aiohttp-sse-client

aiohttp-sse

eth_abi

eth_utils

jsonschema

lightgbm

memory_profiler

orjson

psutil

pytest

scripts

setuptools

tensorflow

textblob

torch

xgboost
